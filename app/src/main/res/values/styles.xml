<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="SettingHeader" parent="@android:style/TextAppearance">
        <item name="android:textSize">12pt</item>
    </style>
    <style name="SensorName" parent="@android:style/TextAppearance">
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">10pt</item>
    </style>
    <style name="SensorEvents" parent="@android:style/TextAppearance">
        <item name="android:textColor">#FF0000</item>
    </style>
    <style name="AddDelButtons" parent="@android:style/TextAppearance">
        android:layout_width="60dp"
        android:layout_height="60dp"
    </style>
</resources>
